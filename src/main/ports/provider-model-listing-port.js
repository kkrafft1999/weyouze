/**
 * Modellauflistung über Provider-Laufzeit (Settings-IPC).
 *
 * @typedef {Object} ProviderModelListingPort
 * @property {(providerId: string, config: object) => Promise<{ models?: Array, error?: string }>} listModels
 */

module.exports = {};
