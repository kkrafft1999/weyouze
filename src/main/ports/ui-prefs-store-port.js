/**
 * UI-Präferenzen-Persistenz.
 *
 * @typedef {Object} UiPrefsStorePort
 * @property {() => Promise<object>} readUIPrefs
 * @property {(updater: (prefs: object) => object|Promise<object>) => Promise<object>} updateUIPrefs
 */

module.exports = {};
