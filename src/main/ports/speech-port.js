/**
 * Spracherkennung (Whisper).
 *
 * @typedef {Object} SpeechPort
 * @property {(audioBuffer: Buffer|ArrayBuffer, options?: { language?: string }) => Promise<{ text?: string, error?: string }>} transcribeAudio
 */

module.exports = {};
